import BotWhatsapp, { addKeyword } from '@bot-whatsapp/bot';

export default addKeyword(BotWhatsapp.EVENTS.ACTION)
.addAnswer('Apoya a mi creador, para que siga mejorando y asiendo mejores chatbots con IA, apoya al avance tecnologico.')

